const {
  getBills,
  getBill,
  createBill,
  updateBill,
  deleteBill,
} = require("./bill.controller");

module.exports = {
  getBills,
  getBill,
  createBill,
  updateBill,
  deleteBill,
};
